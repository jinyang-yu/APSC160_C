/* 02 */
/* Preprocessor Directives */
/* standard input and output.header */
#include <stdio.h>
/* standard library.header */
#include <stdlib.h>
/* when use math function like sin. simple 2*2 doesn't need this */
#include <math.h>
#define GRAV_ACCEL 9.81

/* The main Function
* define a function called main
*/
int main(void)
{
    printf("hi");
    printf("mina");
    system("PAUSE");
    /* ctrl+k+c comment out the whole line: // */
    //return 0; 
    /* the statement terminate the main function */
    return 0;
}
